\begin{table}[t]
    \centering
    \small
    \begin{tabular}{ccccccc}
        \toprule
        \textbf{Run} & \textbf{Patch ($P$)} & \textbf{Stride ($S$)} & \textbf{overlap} & \textbf{\#patch @2048} & \textbf{steps} & \textbf{time} \\
        \midrule
        1 & 16 & 8  & 0.50 & 255 & 100k & 2.6\,h \\
        2 & 16 & 12 & 0.25 & 170 & 100k & 2.8\,h \\
        3 & 16 & 16 & 0.00 & 128 & 100k & pending \\
        4 & 8  & 8  & 0.00 & 256 & 100k & 2.7\,h \\
        5 & 24 & 24 & 0.00 & 86  & 100k & 2.8\,h \\
        \bottomrule
    \end{tabular}
    \caption{Patch/stride configurations retrained from scratch (seed 42) under a uniform $100$k-step budget; \emph{time} is the wall-clock on the RTX 5060 Laptop GPU. The $P{=}16,\,S{=}16$ baseline is retrained under the \emph{same} budget as the variants, so the stride comparison is not confounded by training budget. The official Amazon checkpoint ($200$k steps, full corpus) is used separately, only as an external forecast-quality reference (\autoref{fig:multiFreqPanel}).}
    \label{tab:patchStride}
\end{table}

\section{Data Observations}
Starting from the five geometries of Deliverable~1, the following sections present the observations made from the sweep of the five geometries.

\subsection{Frequency Reconstruction}
Taking into account the frequency of the input tone, the dominant frequency of the rollout is plotted against it in \autoref{fig:reconstruction16}. The diagonal dotted line is a perfect reconstruction, and the dashed verticals are the stride locks $\mathcal F_{lock}$. 

\begin{figure}[b]
    \centering
    \includegraphics[width=0.48\textwidth]{figures/FIG_p16-s16_reconstruction.png}
    \caption{Reconstruction on the $P=16, S=16$ geometry: dominant frequency of the rollout against the tone supplied, averaged over phases, one standard deviation shaded. The dotted diagonal is a tone rebuilt correctly; the dashed verticals are $\mathcal F_{lock}$.}
    \label{fig:reconstruction16}
\end{figure}

Looking at the other geometries in \autoref{fig:reconstruction} we see good and perfect reconstruction for some frequencies, while for some frequencies ranges the output is zero or poorly reconstructed. 

\begin{figure*}[t]
    \centering
    \includegraphics[width=0.48\textwidth]{figures/FIG_p8-s8_reconstruction.png}\hfill
    \includegraphics[width=0.48\textwidth]{figures/FIG_p16-s8_reconstruction.png}\\[6pt]
    \includegraphics[width=0.48\textwidth]{figures/FIG_p16-s12_reconstruction.png}\hfill
    \includegraphics[width=0.48\textwidth]{figures/FIG_p24-s24_reconstruction.png}
    \caption{Reconstruction on the other four geometries: dominant frequency of the rollout against the tone supplied, averaged over phases, one standard deviation shaded. The dotted diagonal is a tone rebuilt correctly; the dashed verticals are $\mathcal F_{lock}$.}
    \label{fig:reconstruction}
\end{figure*}


\subsection{Spectral Recovery}
The spectral recovery of the five geometries is plotted in \autoref{fig:spectralRecovery} and \autoref{fig:spectralRecovery2}. The rows are the seven band tasks and, above the rule, the local $f\pm1$\,Hz task; colour is probe accuracy from chance to one, grey a frequency the task does not cover, dashed verticals $\mathcal F_{lock}$.

\begin{figure*}[!h]
    \centering
    \includegraphics[width=0.90\textwidth]{figures/FIG_p16-s8_spectral_recovery.png}\\[4pt]
    \includegraphics[width=0.90\textwidth]{figures/FIG_p16-s12_spectral_recovery.png}\\[4pt]
    \includegraphics[width=0.90\textwidth]{figures/FIG_p16-s16_spectral_recovery.png}
    \caption{Spectral recovery at $P=16$, the stride lengthening from $S=8$ to $S=16$, so a feature that moves between panels belongs to the stride branch. Rows are the seven band tasks and, above the rule, the local $f\pm1$\,Hz task; colour is probe accuracy from chance to one, grey a frequency the task does not cover, dashed verticals $\mathcal F_{lock}$.}
    \label{fig:spectralRecovery}
\end{figure*}

\begin{figure*}[!h]
    \centering
    \includegraphics[width=0.90\textwidth]{figures/FIG_p8-s8_spectral_recovery.png}\\[4pt]
    \includegraphics[width=0.90\textwidth]{figures/FIG_p24-s24_spectral_recovery.png}
    \caption{Spectral recovery at the two $P=S$ geometries, where the patch length varies at zero overlap and the two branches coincide in both. Rows are the seven band tasks and, above the rule, the local $f\pm1$\,Hz task; colour is probe accuracy from chance to one, grey a frequency the task does not cover, dashed verticals $\mathcal F_{lock}$.}
    \label{fig:spectralRecovery2}
\end{figure*}

\tofill{Collaborating author, target $1000$ words. The observations read off the sweep, in this order: where the recovery curves lose the input frequency across the band and how the loss is distributed over it; that the dips fall on a regular comb rather than at isolated frequencies; that the comb moves when the geometry changes; whether a dip is deeper where a stride lock coincides with a patch null than where only one of the two applies; and how the behavioural and the representational panel agree or disagree. State each as an observation with its figure, and do not anticipate the verdicts: the Bayesian analysis that follows exists to test them.}

\tofill{Collaborating author: the figure pages, from the current run of \texttt{reconstruction\_figures.ipynb}. One full-width page per patch size, grouping that size's strides so that a feature moving between rows belongs to the stride branch; then the two cross-geometry summaries the notebook writes last. Each caption should state what the two panels plot, what changes between rows, and that the dashed lines are $\mathcal F_{lock}$ and the colour scale is probe accuracy from chance to one.}

\subsection{Limits of this design}
The five runs were chosen for Deliverable~1 and they bound what can be concluded from the sweep above. Three of them have $P=S$, so their two branches coincide at every locked frequency and no observation in those geometries can be attributed to one branch. Stride-only sites exist in one geometry, $P{=}16,\,S{=}12$, and patch-only sites in two, so a claim about the stride branch rests on a single configuration and a claim about the patch branch on two. The configuration axes are equally short: the overlap ratio takes three values, and exactly one pair shares a stride while differing in patch size, which is the only place an effect of the patch can be separated from an effect of the stride. Every statement below is therefore conditional on a narrow design, and on one training seed per geometry.

\section{Extending the Sweep}
\label{sec:extension}

The sweep repository now carries every configuration of \autoref{tab:hfModels}, retrained from scratch under the same $100$k-step budget and the same seed as the runs of Deliverable~1. The observations and the fits reported above are those of the five geometries carried over from Deliverable~1; the remainder are available to the final deliverable.

\input{tables/hfModels.tex}
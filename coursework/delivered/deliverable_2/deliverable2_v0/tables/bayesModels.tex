\begin{table}[t]
    \centering
    \small
    \begin{tabular}{@{}llll@{}}
        \toprule
        \textbf{Model} & \textbf{Hypothesis} & \textbf{Estimand} & \textbf{Input} \\
        \midrule
        A & $H1$ behavioural     & $e^{\bar\beta}$: recovery at a lock / at its controls & \texttt{contrasts} \\
        B & $H1$ representational & $e^{\theta_{lock}}$: codelength at a lock / elsewhere & \texttt{mdl\_cells} \\
        C & $H2$                 & $\sigma_\phi$: spread of the deficit across phase & \texttt{contrasts} \\
        D1 & $H3$ location       & which grid the sites lie on (LOO) & \texttt{collapse} \\
        D2 & $H3$ movement       & $\kappa_S$: slope of measured on predicted spacing & \texttt{sites} \\
        \bottomrule
    \end{tabular}
    \caption{The five regressions that make up the analysis. For each one the table gives the hypothesis it addresses, the quantity whose posterior decides that hypothesis, and the table from the probing stage it is fitted to. They are not five views of the same measurement: $H1$ is tested twice, once on what the model forecasts (A) and once on what its representation encodes (B), while $H3$ is split into a question of location, decided by model comparison rather than by a single coefficient (D1), and a question of movement (D2). Models A and C do share an input, but not a response: A uses the phase-averaged contrast $d$ of Deliverable~1, and C uses the same measurement with the phase index kept.}
    \label{tab:bayesModels}
\end{table}

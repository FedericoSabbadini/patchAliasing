This appendix holds two readings that the fitted models of \autoref{sec:four} do not provide, for two different reasons. The first is the work done on the measured photovoltaic series: what the record contains, how it is treated before anything is computed from it, and what the models do when they are handed it. No model is fitted on that series at all, for the reason given in \autoref{sec:five}: it is the application domain of this study and not an experimental corpus. The second is the probe read by depth and by checkpoint, which no fitted model reports because Model~B absorbs the probe stage into a zero-mean offset by design. Nothing here is a hypothesis test and none of the decisions of \autoref{tab:bayesDecisions} depend on it. The purpose is narrower: the synthetic design establishes what a patch grid can and cannot represent at frequencies fixed by $(f_s,P,S)$, and what follows asks what that arithmetic corresponds to in the domain the report is written for, how the models behave on the quantity the agent of \autoref{sec:nine} would read, and where inside the stack frequency information is retained or discarded.

\subsection{What the record contains}

The source is a full calendar year of measurement at one-minute resolution, $525{,}601$ records timestamped in Central European Time and spanning the whole of 2017. Six channels are recorded: module power at a $30^\circ$ tilt, ambient temperature, global horizontal irradiance, global irradiance in the plane of array, wind speed and wind direction. The five weather channels are present for $99.1\%$ of the minutes and carry a large negative sentinel where an instrument returned nothing, which is mapped to a missing value on reading rather than treated as a measurement. Module power is the channel the study uses and the only one whose absence is structural: it is missing from $62.2\%$ of the records, overwhelmingly because the plant is not producing.

That figure decides more than it appears to. The design of \autoref{sec:six} reads a window of $544$ samples, a context of $480$ followed by a horizon of $64$, and a model cannot be handed a window interrupted by an absent sample. Taken literally, only $64$ of the $365$ days carry $544$ consecutive recorded power samples and $97$ reach $480$. A day is therefore not a unit of data here but a container of fragments. Under the convention of the next subsection the constraint relaxes considerably, because a night stops being a gap, but the distinction between a recorded value and a supplied one is carried through every figure rather than dissolved into it.

\subsection{Missing samples and the zero convention}

Any use of this series has to say what a missing power sample means before it can say anything else. Classifying every missing sample by the solar elevation at its own timestamp separates two populations that are indistinguishable in the file itself. In $79.6\%$ of cases the sun is below the horizon: the plant is producing nothing, and zero is not an imputation but the measured quantity. A further $17.2\%$ fall in daylight gaps longer than fifteen minutes, of which seventy-eight runs exceed four hours and account for $49{,}204$ samples on their own; these are plant or logger outages during which nothing was produced and nothing was recorded, and zero stands for them as well. The elevation test is exact rather than heuristic at this site: on the fifteenth day of the year the sun is up between $8.10$ and $16.97$ hours and recorded samples run from $8.15$ to $17.10$, and the two boundaries agree to within a few minutes throughout the year.

The remaining $3.2\%$ are short dropouts inside a working production curve, $6{,}029$ runs with a median length of two minutes. A third of them are flanked by more than fifty watts and their ninetieth percentile flank is $166$ watts, so writing zero into them would stamp an edge down to zero and back within two minutes that the plant never produced. Two things make that edge worse than the absence it replaces. A rectangular notch is broadband in frequency, and a study of which frequencies a patch grid can represent should not manufacture content that lands on the candidate sites of \autoref{eq:locking-candidate}. More seriously, a zero written while irradiance remains valid and high is exactly the pattern \autoref{eq:inverter-trip} defines as \valueof{InverterTrip}, so the flat convention would lead the controller to isolate a photovoltaic side that never failed. These gaps are therefore filled by linear interpolation between their own recorded endpoints and marked as supplied wherever the series is displayed.

The convention is an input convention for forecasting and nothing more. Supplying a value does not make it valid: $v_P(t)$ is unchanged by it, so a supplied sample still falsifies $V_t$, still asserts \valueof{SensorMalfunction} through \autoref{eq:sensor-malfunction}, and still routes the cycle to safe-mode selection. The forecaster receives a series in which absence reads as no production; the controller continues to receive the observability flag recording that the measurement was never made. Collapsing the two would replace an evidence-quality condition with a physical claim, which is the substitution \autoref{eq:sensor-malfunction} exists to prevent. One further class of value is removed before any of this applies: twenty samples across four days report between $1{,}000$ and $59{,}701$ watts against a $99.9$th percentile of $246$, one of them $43{,}730$ watts against a simultaneously measured irradiance of $32$ watts per square metre. They are recording glitches, and they matter beyond their count because the model standardises each context by its own mean and standard deviation, so one of them anywhere in a window flattens the production curve that window was meant to carry. They are treated as missing and then fall under the rules above.

\subsection{Reading a minute-resolution record in the units of the design}

The synthetic design is written at $f_s=512$\,Hz and this record is one sample per minute, and the two are reconciled without resampling, because the quantity carried between them is dimensionless. A tokeniser receives a sequence of samples and has no notion of seconds: a component whose period is $T$ samples, rather than $T$ seconds, completes $P/T$ cycles inside one patch and $S/T$ inside one stride, whatever the wall-clock rate those samples were taken at. Cycles per patch and cycles per stride are therefore the same quantities in both settings, and the lock condition, a whole number of cycles per patch or per stride, is the same predicate in both. A measured window is consequently supplied to a model unaltered and its sample index read as the design reads a sample index.

This is what makes the synthetic result transferable to this domain at all, and it also fixes which real periodicities are at risk. At the reference geometry a candidate site is a periodicity of sixteen minutes, of eight, of five minutes and twenty seconds, and so on down the harmonic series, which is the band in which cloud transients live rather than the band in which the daily cycle does. The horizon sets a second scale that matters throughout: sixty-four samples resolve a frequency grid of $f_s/64$, so a component slower than that completes less than one cycle inside the forecast and cannot be expressed there however well it is represented upstream. The daily cycle is far below that grid, which is why none of the readings that follow is about it.

\subsection{One horizon ahead, at three times of day}

The forecast reading fixes a day and a time of day and asks what the next horizon holds. The horizon is the design's own, $64$ samples, which at this cadence is sixty-four minutes rather than an hour; the distinction is kept because the model's horizon is the fixed quantity. The context is the $2048$ samples immediately preceding the chosen time, taken by position and therefore crossing midnight backwards into the preceding days. Two reasons decide that length rather than a single calendar day: it is the context length the architecture accepts without truncation, and it is the window the retraining procedure of \autoref{sec:appRetraining} used, so the model is asked a question of the shape it was trained on. A multi-day context also contains more than one sunrise, which is the only arrangement in which the daily cycle is a component inside the window rather than the whole of it.

Two checkpoints are read on the identical context: the retrained reference geometry of \autoref{tab:hfModels}, trained from scratch under the common budget on the synthetic mixture, and the published checkpoint at the same $(16,16)$ geometry, which is the stock configuration of the architecture and the reason $(16,16)$ is the reference geometry of the sweep. The comparison is qualified here rather than left to be misread: the published checkpoint was pretrained on a large observational corpus that includes energy and weather series, the retrained one has seen only the synthetic mixture, and any difference between them on measured telemetry is in the first place a difference of training corpus. It is not evidence about structural aliasing in either direction.

One day is read at three cuts, chosen so that the horizon covers three qualitatively different regimes of the production curve: the sunrise ramp, the midday plateau and the afternoon decline. A single cut would not distinguish a model that tracks the plant from one that repeats what it was given, because on a plateau the two behave identically.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/solar_forecast_day186_0545_both.png}
\caption{Context and horizon at the sunrise cut. The left panel is the $2048$-sample context ending at 05:45, spanning three calendar days and showing the missing-data convention in operation: the two full production curves of the preceding days, the nights supplied as zeros, forty-four samples of short daylight gaps filled by interpolation, and only $968$ recorded measurements out of the $2048$ the model receives. The cut falls a few minutes after the plant switches on, so the last ninety minutes of context are flat at zero followed by a step to about $7$ watts. The right panel is the horizon, and at this scale the two forecasts look alike; \autoref{fig:solar0545horizon} shows that they are not.}
\label{fig:solar0545both}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/solar_forecast_day186_0545_horizon.png}
\caption{The sunrise horizon alone, at the scale of the quantity being predicted. The measurement climbs almost linearly from $9$ to $40$ watts. The published median is a horizontal line: it leaves the cut at $7$ watts and ends at $9$, having produced no ramp at all, and its $80\%$ interval is a narrow strip between roughly $5$ and $10$ watts that the measurement crosses out of within three minutes and never re-enters, so the interval contains the plant at none of the $64$ minutes. The retrained median does move, rising to about $12$ watts by the sixteenth minute, but what follows is oscillation between $7$ and $12$ rather than a continued rise. Its interval is several times wider and its upper edge tracks the measurement until roughly the thirtieth minute, which is where its $43$ of $64$ minutes of coverage are spent. Mean absolute error is $11.8$ watts for the retrained checkpoint against a bias of $-11.8$, and $13.4$ against $-13.4$ for the published one: the two coincide, so every minute is wrong in the same direction. The comparison the figure is here for is between the two intervals. The published checkpoint is the more confident of the pair, its band being the narrower by a factor of several, and it is also the further from the plant, so what that narrow band asserts is a certainty the forecast does not have; an $80\%$ interval that excludes the measurement at $64$ of $64$ minutes is not a stricter reading of the same situation but a wrong one. The retrained checkpoint is both closer to the measurement and wider, and it follows the shape of the data more nearly, so for the first half of the horizon the plant remains inside the range the model itself declared. Neither checkpoint anticipates the ramp; they differ in whether they admit it, and only the wider one leaves a controller reading the interval with a true statement to act on.}
\label{fig:solar0545horizon}
\end{figure}

The other two cuts are shown in the form that removes the movement of the production curve itself, since on the level axis a curve that changes by tens of watts inside the horizon makes two very different forecasts look equally close to it. Taking the measurement as the baseline leaves only the error, so a systematic offset appears as a block of bars on one side of the line and a merely noisy forecast alternates across it.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/solar_forecast_day186_1300_error.png}
\caption{Distance from the plant at the midday cut, minute by minute. The measurement is the horizontal line, each bar runs from it to the median of one checkpoint, and each thin line spans that checkpoint's $80\%$ interval. The plant sits between $179$ and $190$ watts and both checkpoints track it closely, at $5.3$ watts of mean absolute error for the retrained one and $3.8$ for the published one, with both intervals containing the measurement at all $64$ minutes. Two features carry over from the sunrise cut. The error drifts with the horizon, the early minutes near zero and the last twenty biased upwards by around $10$ watts, because a flat context produces a flat forecast while the plant is in fact already declining; and the retrained intervals are the wider of the two at every minute, opening to roughly $\pm 35$ watts against $\pm 10$, on a horizon whose true movement is $10$ watts. This is the regime in which the published checkpoint is the more accurate of the pair, and also the one in which the two are hardest to tell apart.}
\label{fig:solar1300err}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/solar_forecast_day186_1700_both.png}
\caption{Context and horizon at the afternoon cut, the one cut of the three whose context carries a strong trend: the ninety minutes before it fall from about $140$ to $73$ watts. This is also the one cut at which the forecasts are not flat. Both checkpoints continue the decline, the published median from $72$ to about $36$ watts and the retrained one from $68$ to about $40$, against a plant that falls from $73$ to $24$. The direction is right and the rate is not: the two reproduce roughly two thirds and three fifths of the true slope. Set against \autoref{fig:solar0545both}, where the context held no trend and both forecasts were flat, the pair identifies what is being continued as the rate of change present in the recent context, and shows that its magnitude is understated.}
\label{fig:solar1700both}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/solar_forecast_day186_1700_error.png}
\caption{The afternoon cut in the error form, where the understated slope of \autoref{fig:solar1700both} becomes a growing one-signed excess. Both checkpoints over-predict, the excess reaches between $15$ and $20$ watts by the end of the horizon, and mean absolute error again coincides with the magnitude of the bias, $6.4$ against $+5.7$ for the retrained checkpoint and $6.9$ against $+6.7$ for the published one. Coverage separates the two as it did at sunrise, at $64$ of $64$ minutes for the retrained intervals and $19$ of $64$ for the published ones. Read with \autoref{fig:solar0545horizon} and \autoref{fig:solar1300err}, this says that the interval of the published checkpoint is calibrated for the plateau and not for the ramps.}
\label{fig:solar1700err}
\end{figure}

Four observations follow from the three cuts and none requires a fit. The failure mode is shared, directional, and better described as an understated rate of change than as a repeated level: where the context carries no trend the forecast is flat and the whole $33$-watt sunrise rise is missed, where it carries a strong one the forecast follows at about two thirds of the slope, and on the plateau a flat context yields a flat forecast against a plant that is already turning. The ordering of accuracy is not stable across the day, the published checkpoint being better at midday by $3.8$ watts against $5.3$ and the retrained one better on both ramps, so a comparison run at one cut would have supported either conclusion. Accuracy and honesty come apart: the published checkpoint is the more accurate at the plateau and the more overconfident everywhere else, with coverage of $0$ and $19$ of $64$ minutes on the two ramps against $43$ and $64$ for the retrained one. And the errors are largest relative to the quantity exactly where the ontology places an anomaly, since a ramp is what \autoref{eq:ramp-event} defines as \valueof{RampEvent}: at sunrise the error is of the same order as the production being predicted, against about two per cent at midday, so the interval a controller would use to decide whether a deviation is real is least trustworthy at the moment the decision matters most.

\textcolor{red}{\textbf{Placeholder, for a figure not yet produced.} The three cuts above compare two checkpoints at one geometry, and the sweep contains fifteen. A single summary figure comparing the forecast behaviour of every model on this series would close the section, and it should report three quantities rather than one, because the cuts above have already shown that they do not move together: mean absolute error, the share of that error attributable to bias, and the fraction of the horizon covered by the model's own $80\%$ interval. Draw it as one panel per quantity with the models on the shared axis and the three cuts distinguished within each model, so that a configuration accurate at midday and uncovered at the ramps is visible as such rather than averaged into a single number. Two cautions belong in its caption. The fifteen retrained geometries are mutually comparable, being trained from scratch under the same budget on the same mixture, so a difference between them is a difference of tokenisation geometry; the published checkpoint is not comparable to them in the same sense and should be drawn as a separate reference rather than as a sixteenth model, since any gap to it is in the first place the corpus difference stated above. And the whole figure remains one day: if it is read across several days, say how many and report the spread across them, because a per-geometry ordering established on a single day would not survive being called a result.}

\textcolor{red}{\textbf{Placeholder, to review and adjust.} These observations rest on three cuts of a single day and should be qualified in whatever direction further cuts take them. Two additions are cheap and worthwhile. Repeating the three cuts on two or three further days from the usable list, ideally including one overcast day whose curve is ragged rather than smooth, would settle whether the coverage failure of the published intervals is a property of ramps in general or of this day. And the upward drift seen at the plateau is either noise, if it reverses on other days, or the same understated-rate behaviour showing up on a curve that is only slightly convex, if it does not. Consider also whether the relative-error contrast, an error of the order of the signal at sunrise against two per cent at midday, belongs in \autoref{sec:ten} rather than only here.}

\subsection{Where the frequency stops being readable}

Model~B of \autoref{sec:appBayes} carries the probe stage as the zero-mean offset $s_{\text{st}}$, which absorbs the several-fold level differences between probe points so that they cannot be attributed to the lock indicator. That is the right treatment for the estimand and it leaves one question unanswered: the fit reports how much the labels cost at a candidate frequency, pooled over the stack, and says nothing about where inside the stack that cost appears. This subsection is the descriptive complement, reporting the same codelengths by probe stage rather than pooled, over the ten states named in \autoref{sec:appBayes}: the encoder $[\mathrm{REG}]$ token after each of the four encoder blocks, the decoder state after each of the four decoder blocks, the pre-projection vector and the output quantile head, in that order.

The profile is read from the archived per-frequency collection rather than from a new measurement, because the cells it needs already exist: one row per probe stage, candidate frequency and geometry, each carrying the codelength charged at that cell and whether the frequency is a candidate or a control. The collection is retrained-weight data built with eighty examples per cell and a context of $480$ samples, and the candidate-frequency construction is identical across the geometries it covers. It is addressed by an immutable revision and its checksum is verified before anything is drawn, so a figure here cannot silently change when the collection does. Codelengths are averaged per stage, separately for candidates and controls, with the spread across cells retained rather than discarded.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/FIG_APPENDIX_F_depth_profile.png}
\caption{Mean local codelength by probe stage at the retrained reference geometry, candidate frequencies against their controls, with bands of one standard deviation across the cells behind each point. The two lines interleave across the four encoder blocks and the sign of their difference changes from stage to stage, so there is no consistent separation before the decoder is entered. From the first decoder block onwards the candidate line lies above the control line at every remaining stage, by roughly twelve bits at the first decoder block, widening to about twenty-five at the third and at the pre-projection vector, and narrowing again to about twelve at the head. The reading is that the additional cost of a candidate frequency appears in the readout rather than at or near tokenisation. The bands overlap everywhere, which is why this locates the effect and does not test it.}
\label{fig:appFdepth}
\end{figure}

The separation could be a property of the reference geometry rather than of the design, so the same profile is repeated across every geometry in the collection. It is drawn as the candidate-minus-control gap rather than as the raw codelength, and the substitution is not cosmetic: Model~B blocks on geometry with the offset $g_{\text{geo}}$ precisely because probe levels differ several-fold between configurations, so a figure of raw levels would show that difference instead of the effect.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{figures/FIG_APPENDIX_F_gap_bypatch.png}
\caption{The candidate-minus-control gap by probe stage, one panel per patch size and one line per stride, with a reference at zero marking no separation. Drawing the fifteen geometries on a single axis made them unreadable; split this way the comparison inside a panel is the one the design varies. Two things are visible across the whole collection. The gap is positive in $147$ of the $150$ stage-by-geometry cells, reaching $49.6$ bits at its largest, so the pattern of \autoref{fig:appFdepth} is general rather than particular to one configuration. And the only three negative cells in the entire collection belong to the reference geometry in its encoder, at $-2.4$, $-4.4$ and $-6.8$ bits, which is the trough visible in the second panel: the configuration the report reads as its reference is the one whose encoder shows the effect least. The mean gap also falls as the patch grows, from $26.7$ bits at $P=8$ through $19.6$ at $P=16$ and at $P=24$ to $10.5$ at $P=32$.}
\label{fig:appFgapPatch}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{figures/FIG_APPENDIX_F_gap_bystride.png}
\caption{The same quantity split the other way, one panel per stride and one line per patch size. The two splits are not equivalent readings of one picture. Ordered by patch size the mean gap decreases monotonically, while ordered by stride it does not: $18.5$ bits at $S=8$, a maximum of $22.2$ at $S=12$, then $13.8$, $12.1$ and $16.1$, and a minimum of $8.2$ at $S=32$. Patch size therefore organises this measurement more cleanly than stride does, which is worth noting because the overlap covariate of \autoref{sec:six} is read along the stride axis at fixed patch size. Neither split makes the depth at which the gap opens stable: nine of the fifteen geometries carry a larger mean gap over the decoder and the head than over the encoder and six carry the reverse, so a single depth cannot be named for the design as a whole.}
\label{fig:appFgapStride}
\end{figure}

\subsection{Band identity by depth and by checkpoint}

The reading above asks a fine question, whether a candidate frequency can be told from its immediate neighbour, on data archived from the retrained sweep. A second and coarser reading asks whether a tone can be placed in the right half of a frequency band, over the seven hierarchical tasks of \autoref{tab:bandTasks}, on states captured while a model actually forecasts. It is measured for both checkpoints in the same pass, on identical generated inputs, seeds, probe stages and estimator, so the checkpoint is the only difference between the two readings. What is reported per task and stage is the space saving, one minus the codelength divided by the cost of coding the same labels uniformly, together with an independent control in which the labels of that same task are permuted over the same features and the same coding chunks. Across both checkpoints the controls stay between $-0.03$ and $0$, so no bar in either figure reports a probe that has memorised its labels.

Space saving cannot reach one, and the value it can reach is worth deriving rather than asserting, because a bar read against one is read against a number the code cannot produce. Let a task carry $n$ examples coded in $C$ chunks $B_0,\dots,B_{C-1}$, as equal as $n$ allows, so that $|B_0|=\lceil n/C\rceil$. The first chunk is transmitted before any probe exists and is charged at one bit per binary label; each later chunk is charged by a probe trained only on the chunks before it, at a cost $L_k=-\sum_{i\in B_k}\log_2\hat p_k(y_i)\ge 0$. The total and the saving are therefore
%
\begin{equation}
\label{eq:sv-ceiling}
L(D)=|B_0|+\sum_{k=1}^{C-1}L_k,
\qquad
SV=1-\frac{L(D)}{n}\;\le\;1-\frac{\lceil n/C\rceil}{n},
\end{equation}
%
and since $n/C\le\lceil n/C\rceil<n/C+1$ the bound lies in $\left(\tfrac{C-1}{C}-\tfrac{C-1}{Cn},\ \tfrac{C-1}{C}\right]$. With the five chunks used here the ceiling is four fifths, and across the seven tasks it runs from $0.79959$ on the smallest to exactly $0.80000$ on the two whose sizes divide by five.

That the ceiling is not one is the point of the estimator rather than a defect of it. Prequential coding charges the labels together with the model that predicts them, so the receiver begins with nothing and the first chunk cannot be predicted by any probe, however perfectly the representation separates the classes. That cost belongs to the coding scheme and is irreducible; what a probe can do is drive the remaining chunks towards zero, which caps the saving at the fraction of the data that is not the first chunk. The bound is moreover a supremum that is not attained, since reaching it would require probability exactly one on every later label. The practical consequence is that a bar at $0.75$ is not three quarters of a perfect reading but ninety-four per cent of what the code allows, and the figures state the derived ceiling in their caption rather than drawing it, since a rule at that value would fall across the per-bar labels.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/FIG_SV_published_p16-s16.png}
\caption{Band-task space saving for the published checkpoint at the decoder states and the head. Through all four decoder blocks every one of the seven tasks sits between $0.75$ and $0.80$, within a few thousandths of the derived ceiling, so band identity is very nearly free to read from those states. At the output head the picture separates by band rather than degrading uniformly: the two lowest tasks fall to $0.336$ and $0.343$, the next two to $0.511$ and $0.571$, while the three high tasks hold at $0.727$, $0.763$ and $0.756$. The projection to the quantile head therefore discards low-frequency band identity and retains high-frequency band identity, which is consistent with the horizon resolving a grid of $f_s/64$: a component near the bottom of the range completes too few cycles inside sixty-four samples for its position to be expressible in the output at all.}
\label{fig:svDecPublished}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/FIG_SV_PAIRED_p16-s16.png}
\caption{The two checkpoints on the same tasks, measured in the same pass. On the left the mean over the seven tasks at each probe point, with the spread across tasks; on the right the difference at every task and stage separately. The published checkpoint is above the retrained one at every stage of the mean, and the difference is concentrated: about $0.13$ to $0.14$ across the four encoder blocks, where the published states sit near $0.77$ and the retrained ones near $0.64$, but only $0.03$ to $0.04$ through the decoder, where both are close to the ceiling. The per-cell panel shows the same asymmetry in more detail: every encoder cell is negative, most strongly on the two middle bands at $-0.28$ and $-0.27$; the decoder cells are near zero; and the output head reverses sign by band, the retrained checkpoint reading the three low tasks better by up to $0.23$ and the four high ones worse. Retraining on the synthetic mixture has therefore cost the encoder much of its general band decodability while leaving the decoder almost untouched, which is a difference of training corpus and not a statement about tokenisation.}
\label{fig:svPaired}
\end{figure}

Three things in this pair are worth carrying into the rest of the report. The two probes answer different questions and their answers do not conflict: coarse band identity survives to the last decoder block in both checkpoints, while the finer candidate-against-neighbour reading of \autoref{fig:appFdepth} already costs more from the first decoder block onwards, so a representation can place a tone in the right half of the spectrum long after it has stopped separating that tone from its immediate neighbour. The encoder difference between checkpoints is the largest effect in either figure and it is a corpus difference, which is worth saying explicitly because the encoder is also where a tokenisation effect would be expected to appear. And the collapse at the head is band-ordered rather than uniform, which is the one observation here that points at the horizon length rather than at the training data.

\textcolor{red}{\textbf{Placeholder, to review and adjust.} Two claims above are stated more firmly than a single run supports. The explanation offered for the band-ordered collapse at the head, that a slow component completes too few cycles inside a sixty-four sample horizon to be expressible there, is a mechanism consistent with the figure and not something the figure establishes; either mark it as a conjecture or test it by repeating the band tasks with the boundaries moved, which predicts the collapse to follow the boundary. The claim that the encoder difference is a corpus difference is an inference from how the two checkpoints were trained, not a measurement, and one retrained checkpoint at a second seed would be the cheapest evidence for it. Decide also whether the contrast between coarse band identity and fine neighbour separation deserves a sentence in \autoref{sec:seven}, since it is the most informative thing this appendix produces and the body does not currently mention it.}

\subsection{Frequency components of the measured series}

The readings above are behavioural or representational and neither says which components of the measured signal survive tokenisation. The decomposition reading asks that directly, on the same series and in the same units, by comparing the frequency content of a measured window with the content of the window a model produces from it. It is the counterpart on real telemetry of the decomposition pages of \autoref{sec:appSweep}, and the two differ in one respect that governs how each may be read: a synthetic window has a ground truth against which a recovered line is either correct or not, and a measured window does not, so what is available here is a comparison between two estimates rather than a measurement against a known value.

\textcolor{red}{\textbf{Placeholder, to be replaced once the decomposition on the measured series is complete.} Report the input side first: which lines the measured window carries once its mean is removed, where they fall in cycles per patch and per stride at the reference geometry, and which coincide with the candidate sites $\mathcal F_{\mathrm{lock}}$ within the resolution the window supports. State that resolution explicitly, since the window length fixes it and it bounds every claim that follows, and note which part of the spectrum is the daily cycle and its harmonics, which lie far below the candidate band. Then the output side: which of those lines survive in the forecast, at what amplitude relative to the input, and whether any line appears that the input did not carry. Report the reference geometry first and then the geometries of \autoref{tab:hfModels}, so that a line lost at one geometry can be checked against a geometry whose combs fall elsewhere; that cross-geometry check is what distinguishes a component the tokenisation cannot represent from one the plant simply stopped producing, and it is the only control available on measured data. Read the result against the two findings already in this appendix rather than in isolation: the horizon resolves a grid of $f_s/64$ and \autoref{fig:svDecPublished} suggests that is already enough to remove low-band identity from the output, so a missing low-frequency line is expected and evidences nothing; and the models were shown to continue an understated rate of change rather than a level, which by itself thins the slowest content of the forecast whatever the tokenisation does. The claim this subsection can support therefore concerns the candidate band alone. Qualify it with the three limits that apply here and not to the synthetic decomposition: there is no ground-truth amplitude, so a recovered line is compared against an estimate carrying its own error; the measured content is not stationary across a window long enough to resolve the candidate band, so a line may weaken for meteorological reasons; and the supplied samples of the zero convention contribute their own content, flat where a night was supplied and smooth where a gap was interpolated, which must not be read as plant behaviour. Conclude by stating what the reading licenses: it is descriptive, carries no interval, and the representational claim of \autoref{sec:four} is settled by the fitted estimands under \autoref{tab:bayesDecisions}. Where it agrees with the synthetic result say it is consistent with it; where it does not, say which of the three limits could account for the difference before treating it as a finding.}

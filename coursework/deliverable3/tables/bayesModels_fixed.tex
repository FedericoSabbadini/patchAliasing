\begin{table*}[!b]
    \centering
    \small
    \begin{tabular}{@{}lllll@{}}
        \toprule
        \textbf{Model} & \textbf{Claim} & \textbf{Estimand} & \textbf{Predicts} & \textbf{Input} \\
        \midrule
        A  & $H1$ behavioural      & $e^{\bar\beta}$: recovery at a lock / at its controls & $e^{\bar\beta}<1$ & paired within-geometry recovery contrasts \\
        B  & $H1$ representational & $e^{\theta_{\mathrm{lock}}}$: codelength at a lock / elsewhere & $e^{\theta_{\mathrm{lock}}}>1$ & fold-contained probe codelengths \\
        C  & $H2$                  & $\sigma_\phi$: spread of the deficit across phase & $\sigma_\phi\approx 0$ & paired within-geometry recovery contrasts \\
        D1 & $H3$ location         & $\theta_S,\theta_P$: deterministic dip depth on each grid, by LOO & $\theta_S,\theta_P<0$ & token-collapse profiles \\
        D2 & $H3a$ stride branch   & $\kappa_S$: measured / stride-predicted site spacing & $\kappa_S=1$ & unambiguous deterministic sites \\
        D2 & $H3b$ patch branch    & $\kappa_P$: measured / patch-predicted site spacing & $\kappa_P=1$ & unambiguous deterministic sites \\
        A$'$ & $M1$ mitigation     & $\delta_O$: cross-geometry change in contrast $d$ per unit of overlap & $\delta_O>0$ & paired recovery contrasts \\
        \bottomrule
    \end{tabular}
    \caption{The models, their estimands, predicted directions and inputs. Models~A and~B are the only models in this set that can evidence a learned failure, respectively in the forecast and in a captured internal state. Models~D1 and~D2 localise deterministic structure induced by patch and stride and do not, by themselves, establish structural aliasing in a learned representation. Model~A$'$ is not a separate fit but a cross-geometry reading of Model~A; its interpretation is qualified by token-count, covered-tail and single-seed limitations.}
    \label{tab:bayesModels}
\end{table*}
